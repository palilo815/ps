#include <bits/stdc++.h>

int main() {
    using namespace std;
    cin.tie(nullptr)->sync_with_stdio(false);
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << 56 * a + 24 * b + 14 * c + 6 * d;
}

